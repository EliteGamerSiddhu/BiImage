This is a very simple and rough image manipulation library. It supports only .bmp file format and is still work in progress.

Alpha v0.1 :-
->Can create white background images with Img.new() function